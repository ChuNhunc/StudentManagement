import { Button, styled } from "@mui/material";

export const CreateButton = styled(Button)({
    background: '#275EFE',
    height: '40px',
    width: '90px',
    color: '#fff',
    borderRadius: 'none',
    "&:hover": {
        backgroundColor: "#0260bd",
    },
});